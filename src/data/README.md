# `src/data/` — static catalog

| File | Role |
|------|------|
| `skills-taxonomy.ts` | **Skill catalog** — names, archetypes, sub-skills, icons |

This is the source of truth for catalog sprints (slug URLs). DB `skills` table should eventually mirror this.

Do not put React components or API calls here.
