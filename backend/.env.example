# LLM (Groq / OpenAI-compatible)
GROQ_API_KEY=

# Supabase
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Upstash Redis — simulation session state (REST)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional: simulation session TTL (seconds, default 14400 = 4 hours)
# SIMULATION_SESSION_TTL_SECONDS=14400

# CORS (comma-separated)
# CORS_ORIGINS=http://localhost:3000
